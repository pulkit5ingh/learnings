# A2.3 — Character-Level LSTM Text Generator

**Phase:** 2 — Machine Learning Core  
**Topic:** Deep Learning, RNNs/LSTMs  
**Type:** Assignment

---

## Goal
Train an LSTM to learn the character-level patterns of a text corpus and generate new text. Understand sequence modeling, hidden states, and temperature sampling.

---

## Dataset
Use any of these (download as plain text):
- Shakespeare's complete works (~5MB)
- A book from Project Gutenberg
- Any large text file you like

---

## Task

### Part 1 — Data Preparation
- Load the text file
- Build a character vocabulary (char → index, index → char)
- Create input/target sequences of length 100 characters
- Convert to PyTorch tensors

### Part 2 — Build the LSTM Model
```python
class CharLSTM(nn.Module):
    # Embedding → LSTM (2 layers, hidden=512) → Linear → output logits
```
- Input: character index
- Output: probability distribution over all characters

### Part 3 — Training
- Loss: CrossEntropyLoss
- Optimizer: Adam, lr=0.003
- Train for 20 epochs on batches of 64 sequences
- Log training loss per epoch

### Part 4 — Text Generation
- Implement a `generate(seed, length, temperature)` function
- Temperature controls randomness:
  - temperature=0.5 → more conservative
  - temperature=1.0 → balanced
  - temperature=1.5 → more creative/random
- Generate 200 characters for each temperature using the same seed

### Part 5 — Analysis
- Show generated samples at 3 different temperatures
- Plot training loss curve
- Write: what does temperature actually do mathematically to the logits?

---

## Deliverables
- [ ] Trained LSTM model
- [ ] Generated text samples at 3 temperatures
- [ ] Training loss curve
- [ ] Written explanation of temperature sampling
