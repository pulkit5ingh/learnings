# How to setup python on our machine ?

- For mac OS
- check if brew is installed ?
- brew --version
- Install brew
- /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
- Install python
- brew install python
- python3 --version
- pip3 --version

# Verify everything

- which python3
  which pip3
  python3 --version
  pip3 --version

# Recommended extensions for python programming in vs code

- Install these extensions:
- Python (Microsoft)
- Pylance
- Python Debugger

# How to setup anaconda in out machine ?

# why do we need anaconda ?

# What is venv in python ?

- This keeps project dependencies isolated.

# What is requirements.txt ?

- A file that lists all Python dependencies and their versions needed to run a project.

# Why is requirements.txt important?

- It helps recreate the exact environment across different machines and deployments.

# What is Uvicorn?

- A lightweight ASGI server used to run FastAPI applications.

# Why does FastAPI use Uvicorn?

- Because FastAPI is ASGI-based and Uvicorn efficiently handles asynchronous requests.

# What is variables in python ?

- variables are container that stores data.
- name = "Pulkit"
  age = 28

# Print statement in python

- print('Hello World")

# What and how is comment works in python ?
