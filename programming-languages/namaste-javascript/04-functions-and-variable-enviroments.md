```javascript

// Sum of two numbers

const Sum = (a,b) => {
    return a+b;
}

console.log(Sum(10,20));

// Convert minutes into seconds
const MinutesIntoSeconds = (minutesValue) => {
    return minutesValue * 60;
}

console.log(MinutesIntoSeconds(2));

// Convert minutes into seconds
const MinutesIntoSeconds = (minutesValue) => {
    return minutesValue * 60;
}

console.log(MinutesIntoSeconds(2));

// Convert age to days
const AgeToDays = (ageValue) => {
    return ageValue * 365;
}

console.log(AgeToDays(2));

// Swap two given numbers!
const swapArrayValues = () => {

}

console.log(AgeToDays(2));

```
