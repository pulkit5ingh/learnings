# A8.4 — Playwright E2E Testing

**Phase:** 8 — Testing
**Topic:** End-to-end testing with Playwright: install, locators, assertions, authenticated flows, CI
**Type:** Assignment

---

## Goal

Write end-to-end tests that simulate real user journeys in the browser using Playwright. You will configure the runner, write page-object-style tests, handle authentication state, and run the suite in GitHub Actions CI.

---

## Background

Playwright is a browser automation framework from Microsoft that supports Chromium, Firefox, and WebKit. Unlike RTL which tests React components in isolation, Playwright drives a real (or headless) browser against your running app.

Key concepts:
- `page.goto(url)` — navigate to a URL
- `page.locator(selector)` — find elements (prefer `getByRole`, `getByText`, `getByLabel`)
- `page.fill()` / `page.click()` — interact with elements
- `expect(locator).toBeVisible()` — Playwright assertions
- `storageState` — persist auth cookies/localStorage between tests
- Fixtures — reusable setup/teardown logic

---

## Tasks

### 1. Install and initialise Playwright

```bash
pnpm dlx create-playwright
# or in an existing Vite project:
pnpm add -D @playwright/test
pnpm exec playwright install --with-deps chromium
```

Running `create-playwright` generates:
- `playwright.config.ts`
- `tests/` directory
- `tests-examples/` (delete these)

### 2. Configure `playwright.config.ts`

```ts
// playwright.config.ts
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  testDir: './e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : undefined,
  reporter: [['html'], ['list']],

  use: {
    baseURL: 'http://localhost:5173',
    trace: 'on-first-retry',
    screenshot: 'only-on-failure',
  },

  projects: [
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
    { name: 'firefox', use: { ...devices['Desktop Firefox'] } },
  ],

  // Start the dev server before running tests
  webServer: {
    command: 'pnpm dev',
    url: 'http://localhost:5173',
    reuseExistingServer: !process.env.CI,
  },
});
```

### 3. Write your first test

```ts
// e2e/home.spec.ts
import { test, expect } from '@playwright/test';

test.describe('Home page', () => {
  test('displays the app title', async ({ page }) => {
    await page.goto('/');
    await expect(page.getByRole('heading', { level: 1 })).toBeVisible();
  });

  test('navigates to About page via nav link', async ({ page }) => {
    await page.goto('/');
    await page.getByRole('link', { name: /about/i }).click();
    await expect(page).toHaveURL('/about');
    await expect(page.getByRole('heading', { name: /about/i })).toBeVisible();
  });
});
```

### 4. Test a form flow

```ts
// e2e/search.spec.ts
import { test, expect } from '@playwright/test';

test('user can search and see results', async ({ page }) => {
  await page.goto('/search');

  const input = page.getByRole('searchbox', { name: /search/i });
  await input.fill('react');
  await input.press('Enter');

  // Wait for results to load
  await expect(page.getByTestId('results-list')).toBeVisible();
  await expect(page.getByRole('listitem')).toHaveCount(10);

  // Click first result
  await page.getByRole('listitem').first().click();
  await expect(page).toHaveURL(/\/detail\//);
});
```

### 5. Authenticated flows with `storageState`

**Step 1: create a global setup file that logs in once**

```ts
// e2e/global-setup.ts
import { chromium, FullConfig } from '@playwright/test';

async function globalSetup(config: FullConfig) {
  const { baseURL } = config.projects[0].use;
  const browser = await chromium.launch();
  const page = await browser.newPage();

  await page.goto(`${baseURL}/login`);
  await page.getByLabel('Email').fill('test@example.com');
  await page.getByLabel('Password').fill('password123');
  await page.getByRole('button', { name: /sign in/i }).click();
  await page.waitForURL('**/dashboard');

  // Save auth state (cookies + localStorage)
  await page.context().storageState({ path: 'e2e/.auth/user.json' });
  await browser.close();
}

export default globalSetup;
```

**Step 2: reference it in the config**

```ts
// playwright.config.ts (additions)
export default defineConfig({
  globalSetup: './e2e/global-setup.ts',

  projects: [
    // Setup project that runs first
    {
      name: 'setup',
      testMatch: /global-setup\.ts/,
    },
    // Authenticated tests reuse the stored state
    {
      name: 'chromium-auth',
      use: {
        ...devices['Desktop Chrome'],
        storageState: 'e2e/.auth/user.json',
      },
      dependencies: ['setup'],
    },
  ],
});
```

**Step 3: write an authenticated test**

```ts
// e2e/dashboard.spec.ts
import { test, expect } from '@playwright/test';

// This test file runs in the 'chromium-auth' project
test('authenticated user sees dashboard', async ({ page }) => {
  await page.goto('/dashboard');
  // Should NOT be redirected to /login
  await expect(page).toHaveURL('/dashboard');
  await expect(page.getByText(/welcome/i)).toBeVisible();
});

test('user can create a new item', async ({ page }) => {
  await page.goto('/dashboard');
  await page.getByRole('button', { name: /new item/i }).click();

  const dialog = page.getByRole('dialog');
  await expect(dialog).toBeVisible();

  await dialog.getByLabel('Title').fill('My test item');
  await dialog.getByRole('button', { name: /save/i }).click();

  await expect(dialog).not.toBeVisible();
  await expect(page.getByText('My test item')).toBeVisible();
});
```

### 6. Page Object Model (POM) for maintainability

```ts
// e2e/pages/LoginPage.ts
import { Page, Locator } from '@playwright/test';

export class LoginPage {
  readonly page: Page;
  readonly emailInput: Locator;
  readonly passwordInput: Locator;
  readonly submitButton: Locator;

  constructor(page: Page) {
    this.page = page;
    this.emailInput = page.getByLabel('Email');
    this.passwordInput = page.getByLabel('Password');
    this.submitButton = page.getByRole('button', { name: /sign in/i });
  }

  async goto() {
    await this.page.goto('/login');
  }

  async login(email: string, password: string) {
    await this.emailInput.fill(email);
    await this.passwordInput.fill(password);
    await this.submitButton.click();
  }
}
```

```ts
// e2e/login.spec.ts
import { test, expect } from '@playwright/test';
import { LoginPage } from './pages/LoginPage';

test('shows error with wrong credentials', async ({ page }) => {
  const loginPage = new LoginPage(page);
  await loginPage.goto();
  await loginPage.login('wrong@example.com', 'bad-pass');

  await expect(page.getByRole('alert')).toContainText(/invalid/i);
  await expect(page).toHaveURL('/login');
});
```

### 7. CI integration with GitHub Actions

```yaml
# .github/workflows/e2e.yml
name: E2E Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v3
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install

      - name: Install Playwright browsers
        run: pnpm exec playwright install --with-deps chromium

      - name: Run Playwright tests
        run: pnpm exec playwright test
        env:
          CI: true

      - name: Upload test report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
```

### 8. Useful Playwright CLI commands

```bash
# Run all tests
pnpm exec playwright test

# Run tests in UI mode (interactive)
pnpm exec playwright test --ui

# Run a specific file
pnpm exec playwright test e2e/search.spec.ts

# Debug a test (opens browser + Playwright inspector)
pnpm exec playwright test --debug

# Show last HTML report
pnpm exec playwright show-report

# Generate test code by clicking (codegen)
pnpm exec playwright codegen http://localhost:5173
```

---

## Deliverables

- [ ] `playwright.config.ts` with `webServer`, two browser projects, and HTML reporter
- [ ] At least 3 test files in `e2e/` covering navigation, a form flow, and an auth-protected page
- [ ] `global-setup.ts` that logs in and saves `storageState` to `e2e/.auth/user.json`
- [ ] At least one Page Object class in `e2e/pages/`
- [ ] `.github/workflows/e2e.yml` that installs deps, installs browsers, and runs `playwright test`
- [ ] All tests pass locally with `pnpm exec playwright test`
- [ ] Test artifacts (screenshots, traces) uploaded as CI artifacts on failure
