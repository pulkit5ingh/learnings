# A7.1 — Prometheus + Grafana: Instrument and Visualize an App

**Phase:** 7 — Monitoring, Logging & Observability  
**Topic:** Metrics, Prometheus, Grafana, PromQL  
**Type:** Assignment

---

## Goal
Set up a full metrics stack and instrument a real application. Observability is what separates good engineers from great ones — you need to know when things break before your users tell you.

---

## Task

### Part 1 — Run the Stack with Docker Compose
```yaml
# docker-compose.monitoring.yml
version: '3.9'

services:
  prometheus:
    image: prom/prometheus:latest
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.retention.time=15d'
      - '--web.enable-lifecycle'  # allow config reload via API

  grafana:
    image: grafana/grafana:latest
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
      - grafana_data:/var/lib/grafana
      - ./grafana/dashboards:/etc/grafana/provisioning/dashboards
      - ./grafana/datasources:/etc/grafana/provisioning/datasources

  node_exporter:
    image: prom/node-exporter:latest
    ports:
      - "9100:9100"
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
    command:
      - '--path.procfs=/host/proc'
      - '--path.rootfs=/rootfs'
      - '--path.sysfs=/host/sys'

volumes:
  prometheus_data:
  grafana_data:
```

```yaml
# prometheus.yml
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'node'
    static_configs:
      - targets: ['node_exporter:9100']

  - job_name: 'myapp'
    static_configs:
      - targets: ['myapp:8080']
    metrics_path: /metrics
```

### Part 2 — Instrument a Python Application
```python
# app.py
from prometheus_client import Counter, Histogram, Gauge, generate_latest, CONTENT_TYPE_LATEST
from fastapi import FastAPI, Request
import time

app = FastAPI()

# Counters — only go up
http_requests_total = Counter(
    'http_requests_total',
    'Total HTTP requests',
    ['method', 'endpoint', 'status']
)

# Histograms — for measuring durations/sizes
request_duration_seconds = Histogram(
    'request_duration_seconds',
    'HTTP request duration',
    ['method', 'endpoint'],
    buckets=[0.01, 0.05, 0.1, 0.25, 0.5, 1.0, 2.5, 5.0]
)

# Gauges — can go up and down
active_connections = Gauge('active_connections', 'Active connections')
queue_size = Gauge('queue_size', 'Current queue size', ['queue_name'])

@app.middleware("http")
async def prometheus_middleware(request: Request, call_next):
    active_connections.inc()
    start_time = time.time()
    
    response = await call_next(request)
    
    duration = time.time() - start_time
    status = str(response.status_code)
    
    http_requests_total.labels(
        method=request.method,
        endpoint=request.url.path,
        status=status
    ).inc()
    
    request_duration_seconds.labels(
        method=request.method,
        endpoint=request.url.path
    ).observe(duration)
    
    active_connections.dec()
    return response

@app.get("/metrics")
def metrics():
    return Response(generate_latest(), media_type=CONTENT_TYPE_LATEST)

@app.get("/api/users")
def get_users():
    # Simulate variable latency
    time.sleep(random.uniform(0.01, 0.3))
    return {"users": ["alice", "bob"]}
```

### Part 3 — PromQL Queries
```promql
# Request rate (requests per second)
rate(http_requests_total[5m])

# Error rate (only 5xx responses)
rate(http_requests_total{status=~"5.."}[5m])

# Error rate as percentage
rate(http_requests_total{status=~"5.."}[5m]) 
  / rate(http_requests_total[5m]) * 100

# 95th percentile latency
histogram_quantile(0.95, 
  rate(request_duration_seconds_bucket[5m])
)

# CPU usage across all cores
100 - (avg by (instance) (rate(node_cpu_seconds_total{mode="idle"}[5m])) * 100)

# Memory available
node_memory_MemAvailable_bytes / node_memory_MemTotal_bytes * 100

# Disk I/O read bytes per second
rate(node_disk_read_bytes_total[5m])
```

### Part 4 — Build Grafana Dashboards
Create a dashboard with these panels:

1. **Request Rate** (time series): `rate(http_requests_total[5m])`
2. **Error Rate %** (stat): 
3. **P50/P95/P99 Latency** (time series): 3 queries with different quantiles
4. **Active Connections** (gauge): `active_connections`
5. **CPU Usage** (time series): per core and average
6. **Memory Usage** (bar chart): used vs available
7. **Disk Usage** (stat): filesystem used %

Export the dashboard JSON and commit to git.

### Part 5 — Grafana Provisioning (Config as Code)
```yaml
# grafana/datasources/prometheus.yml
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    url: http://prometheus:9090
    isDefault: true
    editable: false
```

```yaml
# grafana/dashboards/dashboard.yml
apiVersion: 1
providers:
  - name: 'default'
    folder: ''
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards
```

---

## Deliverables
- [ ] Prometheus, Grafana, Node Exporter running via Docker Compose
- [ ] App instrumented with Counter, Histogram, Gauge
- [ ] `/metrics` endpoint returning valid Prometheus format
- [ ] Prometheus scraping app metrics (check at localhost:9090/targets)
- [ ] 7-panel Grafana dashboard created
- [ ] Dashboard exported to JSON and committed to git
- [ ] Grafana provisioned from config files (no manual setup after `docker compose up`)
- [ ] PromQL: error rate query returning correct percentage
